import 'package:flutter/material.dart';
import 'package:flutter_screenutil/flutter_screenutil.dart';

class Gap5 extends StatelessWidget {
  const Gap5({super.key, this.isHorizontal});

  final bool? isHorizontal;

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: isHorizontal == null ? 5.h : 0,
      width: isHorizontal == true ? 5.w : 0,
    );
  }
}

class Gap10 extends StatelessWidget {
  const Gap10({super.key, this.isHorizontal});

  final bool? isHorizontal;

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: isHorizontal == null ? 10.h : 0,
      width: isHorizontal == true ? 10.w : 0,
    );
  }
}

class Gap15 extends StatelessWidget {
  const Gap15({super.key, this.isHorizontal});

  final bool? isHorizontal;

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: isHorizontal == null ? 15.h : 0,
      width: isHorizontal == true ? 15.w : 0,
    );
  }
}

class Gap20 extends StatelessWidget {
  const Gap20({super.key, this.isHorizontal});

  final bool? isHorizontal;

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: isHorizontal == null ? 20.h : 0,
      width: isHorizontal == true ? 20.w : 0,
    );
  }
}

class Gap25 extends StatelessWidget {
  const Gap25({super.key, this.isHorizontal});

  final bool? isHorizontal;

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: isHorizontal == null ? 25.h : 0,
      width: isHorizontal == true ? 25.w : 0,
    );
  }
}

class Gap30 extends StatelessWidget {
  const Gap30({super.key, this.isHorizontal});

  final bool? isHorizontal;

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: isHorizontal == null ? 30.h : 0,
      width: isHorizontal == true ? 30.w : 0,
    );
  }
}

class Gap35 extends StatelessWidget {
  const Gap35({super.key, this.isHorizontal});

  final bool? isHorizontal;

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: isHorizontal == null ? 35.h : 0,
      width: isHorizontal == true ? 35.h : 0,
    );
  }
}

class Gap40 extends StatelessWidget {
  const Gap40({super.key, this.isHorizontal});

  final bool? isHorizontal;

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: isHorizontal == null ? 40.h : 0,
      width: isHorizontal == true ? 40.w : 0,
    );
  }
}

class Gap45 extends StatelessWidget {
  const Gap45({super.key, this.isHorizontal});

  final bool? isHorizontal;

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: isHorizontal == null ? 45.h : 0,
      width: isHorizontal == true ? 45.h : 0,
    );
  }
}

class Gap50 extends StatelessWidget {
  const Gap50({super.key, this.isHorizontal});

  final bool? isHorizontal;

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: isHorizontal == null ? 50.h : 0,
      width: isHorizontal == true ? 50.w : 0,
    );
  }
}

class Gap55 extends StatelessWidget {
  const Gap55({super.key, this.isHorizontal});

  final bool? isHorizontal;

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: isHorizontal == null ? 55.h : 0,
      width: isHorizontal == true ? 55.h : 0,
    );
  }
}

class Gap60 extends StatelessWidget {
  const Gap60({super.key, this.isHorizontal});

  final bool? isHorizontal;

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: isHorizontal == null ? 60.h : 0,
      width: isHorizontal == true ? 60.w : 0,
    );
  }
}

class Gap65 extends StatelessWidget {
  const Gap65({super.key, this.isHorizontal});

  final bool? isHorizontal;

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: isHorizontal == null ? 65.h : 0,
      width: isHorizontal == true ? 65.h : 0,
    );
  }
}

class Gap70 extends StatelessWidget {
  const Gap70({super.key, this.isHorizontal});

  final bool? isHorizontal;

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: isHorizontal == null ? 70.h : 0,
      width: isHorizontal == true ? 70.h : 0,
    );
  }
}

class Gap75 extends StatelessWidget {
  const Gap75({super.key, this.isHorizontal});

  final bool? isHorizontal;

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: isHorizontal == null ? 75.h : 0,
      width: isHorizontal == true ? 75.h : 0,
    );
  }
}

class Gap80 extends StatelessWidget {
  const Gap80({super.key, this.isHorizontal});

  final bool? isHorizontal;

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: isHorizontal == null ? 80.h : 0,
      width: isHorizontal == true ? 80.h : 0,
    );
  }
}

class Gap85 extends StatelessWidget {
  const Gap85({super.key, this.isHorizontal});

  final bool? isHorizontal;

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: isHorizontal == null ? 85.h : 0,
      width: isHorizontal == true ? 85.h : 0,
    );
  }
}

class Gap90 extends StatelessWidget {
  const Gap90({super.key, this.isHorizontal});

  final bool? isHorizontal;

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: isHorizontal == null ? 90.h : 0,
      width: isHorizontal == true ? 90.h : 0,
    );
  }
}

class Gap95 extends StatelessWidget {
  const Gap95({super.key, this.isHorizontal});

  final bool? isHorizontal;

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: isHorizontal == null ? 95.h : 0,
      width: isHorizontal == true ? 95.h : 0,
    );
  }
}

class Gap100 extends StatelessWidget {
  const Gap100({super.key, this.isHorizontal});

  final bool? isHorizontal;

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: isHorizontal == null ? 100.h : 0,
      width: isHorizontal == true ? 100.h : 0,
    );
  }
}
